# 🎣 钓鱼池游戏 - 快速开始

## 一键Setup

```bash
bin/setup
```

这会自动：
- ✅ 检查Ruby环境
- ✅ 安装依赖
- ✅ 创建必要的目录
- ✅ 运行测试验证

## 立即游玩

### 1. 快速演示（4个随机玩家）

```bash
bin/play --demo
```

输出示例：
```
🎣 Fishing Pond Game
============================================================
Players: Alice, Bob, Carol, Dave
Rounds: 4
Mode: Batch
============================================================

🏆 Winner: Alice!

💭 Strategy Thoughts (会显示所有玩家的思考过程)
```

### 2. 体验所有策略 + 生成HTML报告 ⭐ 推荐

```bash
bin/play --dry-run --rounds 4 --reports
```

这会：
- 🎮 6个示例策略对战（Conservative, Greedy, TitForTat, Adaptive, Punisher, Random）
- 🎯 运行4个回合
- 📊 每回合生成HTML报告到 `reports/` 目录

然后查看报告：
```bash
open reports/round_1.html    # macOS
xdg-open reports/round_1.html  # Linux
```

### 3. 交互模式（适合团队会议）

```bash
bin/play --dry-run --interactive --rounds 6 --reports
```

每回合后会暂停，按回车继续：
```
Round 1 complete. Press Enter to continue, 'q' to quit, or 'r' for report:
```

适合边讨论边进行！

## HTML报告内容

打开 `reports/round_1.html` 你会看到：

### 📊 Round Summary
- 配对数量
- 互选情况统计

### 🤝 Pairings
- 每对玩家的详细对战记录
- 10回合的逐回合数据
- 成功/过度捕捞标识
- 池塘鱼数变化

### 🏆 Leaderboard
- 当前排名（带奖牌 🥇🥈🥉）
- 总分

### 📈 Strategy Insights
- 合作率
- 池塘枯竭率
- 最贪婪/最保守玩家

### 💭 Strategy Thoughts ⭐ 新功能
- 每个玩家的完整思考过程
- 按回合和阶段组织
- 了解每个决策背后的逻辑

## 自定义游戏

### 调整回合数

```bash
bin/play --dry-run --rounds 10 --reports
```

### 只显示终端输出（不生成HTML）

```bash
bin/play --dry-run --rounds 4
# 会在终端显示完整的thoughts
```

### 批处理模式（最快）

```bash
bin/play --batch --dry-run --rounds 4
```

## 验证安装

```bash
# 运行所有测试
bundle exec rspec

# 查看测试详情
bundle exec rspec --format documentation
```

应该看到：
```
86 examples, 0 failures
```

## 下一步

1. 📖 阅读 `CLAUDE.md` 了解完整游戏规则
2. 📝 参考 `lib/strategy/examples/` 学习策略编写
3. 🎯 创建自己的策略在 `strategies/` 目录
4. ✅ 用 `bin/validate` 验证策略
5. 🏆 参加团队挑战！

## 常见问题

### Q: 如何查看帮助？
```bash
bin/play --help
```

### Q: 测试失败怎么办？
游戏仍然可以玩，测试失败通常不影响核心功能。

### Q: 如何创建自己的策略？
参考 `README.md` 中的"Writing Your Strategy"部分。

### Q: HTML报告在哪里？
`reports/` 目录，每回合一个文件 `round_N.html`

## 享受游戏！🎉
